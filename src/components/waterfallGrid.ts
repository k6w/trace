/* One grid, two consumers. The header row and every data row read the same
   template from here — when they drifted apart the column labels collided. */
export const WATERFALL_GRID =
  'minmax(0,44px) minmax(0,46px) minmax(0,1fr) minmax(0,46px) minmax(0,76px) minmax(0,42%)'

export const WATERFALL_GAP = '12px'

export const ROW_HEIGHT = 30
